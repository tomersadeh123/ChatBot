# ============================================================================
# GROQ API KEYS (Multiple keys for rotation)
# ============================================================================
# Option 1: Comma-separated list
GROQ_API_KEY=key1,key2,key3

# Option 2: Numbered keys (uncomment to use)
# GROQ_API_KEY_1=your_first_groq_key_here
# GROQ_API_KEY_2=your_second_groq_key_here
# GROQ_API_KEY_3=your_third_groq_key_here

# ============================================================================
# GROQ CONFIGURATION
# ============================================================================
# Model to use (default: llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile

# API timeout in seconds (default: 30)
GROQ_TIMEOUT=30

# Maximum tokens in response (default: 350)
GROQ_MAX_TOKENS=350

# Temperature for responses 0.0-1.0 (default: 0.7)
GROQ_TEMPERATURE=0.7

# Key rotation settings
GROQ_KEY_RATE_PER_MIN=30            # Requests per minute per key (default: 30)
GROQ_KEY_INTERVAL_SECONDS=2          # Min interval between requests (default: 2)

# ============================================================================
# RAG (Retrieval-Augmented Generation) SETTINGS
# ============================================================================
# Text chunking settings
RAG_CHUNK_SIZE=500                   # Words per chunk (default: 500)
RAG_CHUNK_OVERLAP=50                 # Overlapping words (default: 50)
RAG_TOP_K=4                          # Number of results to retrieve (default: 4)

# Embedding model (default: paraphrase-MiniLM-L3-v2)
RAG_EMBEDDING_MODEL=paraphrase-MiniLM-L3-v2

# ============================================================================
# MONGODB LOGGING (Optional - works without it)
# ============================================================================
# Get free MongoDB Atlas cluster at https://www.mongodb.com/cloud/atlas
# Connection string format: mongodb+srv://username:password@cluster.mongodb.net/

MONGODB_URI=your_mongodb_connection_string_here

# Database and collection names (optional, has defaults)
CHATBOT_MONGO_DB=chatbot_db
CHATBOT_MONGO_COLLECTION=chat_logs
CHATBOT_MONGO_SESS_COLLECTION=chat_sessions

# ============================================================================
# GITHUB INTEGRATION
# ============================================================================
# Your GitHub username for auto-syncing repositories
GITHUB_USERNAME=your_github_username

# ============================================================================
# FLASK APPLICATION SETTINGS
# ============================================================================
# Port for local development (default: 5000)
PORT=5000

# Debug mode (default: False)
FLASK_DEBUG=False

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# 1. GROQ API Keys:
#    - Get free keys from https://console.groq.com
#    - Use multiple keys for higher rate limits (recommended 3-5 keys)
#
# 2. MongoDB (Optional but recommended):
#    - Free tier: https://www.mongodb.com/cloud/atlas/register
#    - Create M0 cluster (512MB, free forever)
#    - Add database user and get connection string
#    - Whitelist IP: 0.0.0.0/0 for cloud deployments
#
# 3. For Render deployment:
#    - Add all environment variables in Render dashboard
#    - Separate multiple GROQ_API_KEY values with commas
#    - Or use numbered keys: GROQ_API_KEY_1, GROQ_API_KEY_2, etc.
